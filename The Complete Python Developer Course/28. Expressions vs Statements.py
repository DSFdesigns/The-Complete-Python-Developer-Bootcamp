# expression (produces a value) as iq / 5
# statements (performs an action) user_age = iq / 5
# iq = 100 is also a statement
iq = 100 # This is a statement
user_age = iq / 5 # This is an expression

print(user_age)
